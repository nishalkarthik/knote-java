pipeline {
    agent { label 'slave' } // Use the agent with the label 'slave'
    stages {
        stage('Print Message') {
            steps {
                // Use the sh step to execute a shell command that prints "Hello Hii"
                sh 'mkdir newlyyy'
            }
        }
    }
}

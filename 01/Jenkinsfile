pipeline {
    stages {
        label 'any' 
    }
        stage('Build') {
            steps {
                sh 'ls -la'
                sh 'cat /etc/os-release'
                sh ' cat 01/Jenkinsfile'
            }
        }
    }
}
